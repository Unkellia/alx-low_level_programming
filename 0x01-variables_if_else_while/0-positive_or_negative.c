#include <stdio.h>
#include <stdlib.h>

/**
 *  * main - main block
 *   */

void main(){
	int n

scanf("%d", &n)
if(n>0)
	        printf(n "is positive\n");
else if(n == 0)
	        printf(n "is zero\n");
else if(n<0)
	        printf(n "is negative\n");
